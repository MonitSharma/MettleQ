# MettleQ plot relabel bundle

These plots were regenerated after the package rename from frozen pre-rename
CSV/JSON measurements. Numerical values and raw evidence were not rewritten.

| Plot | Frozen source |
| --- | --- |
| `sdk_method_matrix.png` | Step 6 `sdk_method_matrix_summary.json` |
| `sdk_adapter_step5.png` | Step 5 `sdk_adapter_summary.json` |
| `sdk_adapter_timings.png` | Step 6 adapter-refresh summary |
| `mps_limit_landscape.png` | Step 8 standard/boundary raw CSVs |
| `mps_dmax_convergence.png` | Step 8 Dmax raw CSVs |
| `matched_aer_comparison.png` | Step 8 matched-Aer summary JSON |

The plot scripts accept both the historical `qupertino_*` schema and the
canonical `mettleq_*` schema. Historical source bundles remain immutable.
